
'use client'
import { useEffect } from 'react'

export default function ChatWidget() {
  useEffect(()=>{
    // If Crisp is loaded it will handle the widget. This is a fallback for custom chat later.
  },[])

  return null
}
